# 영어시험 자동 채점 앱 - 환경 설정
# 이 파일을 .env로 복사하여 사용하세요

# 서버 설정
PORT=3001
NODE_ENV=development
HOST=0.0.0.0

# JWT 설정
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Google Cloud Vision API
# Google Cloud Console에서 서비스 계정 키 JSON 파일 경로
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# OpenAI API (GPT-4 Vision 채점용)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o

# 파일 업로드 설정
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
MAX_FILES=10

# 데이터베이스
DB_PATH=./data/exam_grading.db

# CORS 허용 출처
ALLOWED_ORIGINS=http://localhost:19006,http://localhost:8081,exp://192.168.5.228:8081

# 채점 설정
FUZZY_MATCH_THRESHOLD=0.7
SHORT_ANSWER_THRESHOLD=0.8
